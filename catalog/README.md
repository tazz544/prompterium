# Catalog

Canonical Cursor context for app repos. Paths here mirror `.cursor/` after running [`../scripts/sync.sh`](../scripts/sync.sh).

Add shareable **skills** as `skills/<name>/SKILL.md` (folder name matches frontmatter `name`). Add **rules** as `rules/*.mdc`, **hooks** under `hooks/`, and **templates** under `templates/`.
